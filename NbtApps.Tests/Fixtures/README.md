# Compilation of test fixtures
* Fixture files that are needed by the tests need to have their "Copy to Output Directory" set to "Copy if newer".
* The solution must be cleaned up for the fixtures to be included.